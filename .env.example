# Origem do backend, acessada somente pelo servidor do frontend.
# Copie este arquivo para .env.local. Obrigatório em produção.
FINFLOW_API_URL=http://localhost:8080

# Login e cadastro usam /api/auth; as demais rotas usam /api/v1.
# A autenticação é por JWT de cada usuário. Nenhum segredo JWT é necessário aqui.
